apiVersion: v1
kind: Pod
metadata:
  name: hacker-pod
  namespace: default
  labels:
    app: demo
spec:
  serviceAccountName: demo-sa
  containers:
  - name: kubectl
    image: bitnami/kubectl:latest
    command: ["sleep", "3600"]